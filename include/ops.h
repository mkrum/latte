#ifndef OPS_H
#define OPS_H

#include <string>
#include <stdlib.h>
#include <assert.h>

namespace utils {
  bool is_double(const char* str);
  double string_to_double(std::string double_val);
}

#endif
